name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build and test (macOS)
    # Unpinned because this package declares Swift tools version 6.2, which the
    # macos-15 image does not ship. The other libraries pin macos-15.
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show environment
        run: |
          xcodebuild -version
          swift --version

      - name: Build
        run: swift build

      - name: Test
        run: swift test

      - name: Build with complete strict concurrency
        run: swift build --build-tests -Xswiftc -strict-concurrency=complete
